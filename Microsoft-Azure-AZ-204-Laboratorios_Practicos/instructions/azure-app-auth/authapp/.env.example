# Example .env file — do NOT commit real secrets. Use this as template.
CLIENT_ID=YOUR_CLIENT_ID
TENANT_ID=YOUR_TENANT_ID
# If your app uses a client secret, store it securely and do NOT commit it.
# CLIENT_SECRET=YOUR_CLIENT_SECRET